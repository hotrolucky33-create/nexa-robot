# Self-test report

Initial MVP checks:

- unavailable simulation blocks verification;
- unavailable manufacturing rules block verification;
- CAD/drawing mismatch fails;
- missing material provenance is `UNKNOWN` and blocks verification.

Known limitations: no real CAD kernel, solver, queue, payment gateway, signed package generation, authentication, or human-review workflow is connected yet.
