# Verification engine

Verification is a conjunction of required checks. `PASS` requires actual evidence; `FAIL`, `UNKNOWN`, and `NOT_AVAILABLE` block release. Diagnostic scores cannot override a required check. Failure reports contain check IDs, severity, actual evidence, and a stable category.
