# FEA engine

`ExternalSolverProvider` is the adapter boundary for CalculiX, Code_Aster, or another reviewed solver. It reports `NOT_AVAILABLE` without a configured solver. It does not synthesize stress or convergence output.
