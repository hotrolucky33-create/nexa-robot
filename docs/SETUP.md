# Setup

Install Node.js 22+, copy `.env.example` to `.env`, run `npm install`, then `npm run self-test` and `npm start`.

FreeCAD, a solver, PostgreSQL, Redis, S3, and payment credentials are not required for this local MVP. Their absence is represented as `NOT_AVAILABLE`, and affected products cannot be verified.
