# Engineering benchmarks

Benchmarks contain known inputs and expected numerical ranges. A benchmark result is evidence only when the implementation runs and stays within its declared tolerance. AI output is not used as an oracle.

Current benchmark: axial shaft stress in `calculations/shaft-reference.json`.
